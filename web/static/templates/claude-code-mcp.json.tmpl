{
  "mcpServers": {
    "memento": {
      "command": "{{MCP_BINARY_PATH}}",
      "args": [],
      "env": {
        "MEMENTO_DATA_PATH": "{{DATA_DIR}}",
        "MEMENTO_CONNECTIONS_CONFIG": "{{CONNECTIONS_CONFIG}}"
      }
    }
  }
}
